// before
let string = "Hello World!";

// used built-in method substring(startIndex, endIndex) to extract a portion of the string
let substring = string.substring(0, 5);
console.log("after:", substring);
