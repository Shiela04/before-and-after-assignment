// before
let string = "Shiela Daguio";
console.log("before:", string);

// used built-in method length to count how many characters the string contains including spaces
let length = string.length;
console.log("after:", length);
